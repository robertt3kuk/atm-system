#include "stdio.h"

#define MAX 256
const char *RECORDS = ".records.txt";

int main() {
  FILE *fptr1, *fptr2;
  int lno, linectr = 0;
  char str[MAX], fnmae[MAX];
  char newln[MAX], temp[] = "temp.txt";
}
